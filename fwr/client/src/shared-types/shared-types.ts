import { match } from "react-router-dom";

// export interface RouteComponentProps<Params extends { [K in keyof Params]?: string } = {}, S = H.LocationState> {
//   history: H.History;
//   location: H.Location<S>;
//   match: match<Params>;
// }